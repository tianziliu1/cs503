{
    "version": "2.0.0",
    "tasks": [
      {
      "label": "Build with Make",
      "type": "shell",
      "command": "make",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options":{
        "cwd": "${cs503}"
      },
      "problemMatcher": ["$gcc"],
      "detail": "Runs the 'make' command to build the project."
      }
    ],
    
}
